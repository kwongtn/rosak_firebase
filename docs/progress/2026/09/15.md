# Progress Log — 2026-09-15

## Summary

Fixed both failing GitHub Actions workflows (`CI`, `Deploy Functions`). Full suite green: 55 files, 418 tests.

---

## Commits by Module

### console / core (CI test fixes)

| Commit      | Type | Message                                                                                |
| ----------- | ---- | -------------------------------------------------------------------------------------- |
| _(pending)_ | fix  | Mock `graphqlResource` HTTP in console specs; stub Firebase-touching services in tests |

### functions / deploy (Deploy Functions fixes)

| Commit      | Type | Message                                                                     |
| ----------- | ---- | --------------------------------------------------------------------------- |
| _(pending)_ | fix  | Resync functions lockfile, Node 18→20, add firebase.json functions codebase |

---

## Detailed Changes

### CI: unmocked `graphqlResource` HttpClient in console specs

- `links.component.spec.ts`, `pending.component.spec.ts`: added `provideHttpClientTesting()` and flushed the reference-data POST (the `GraphQLClient` mock never covered `graphqlResource`'s own `HttpClient`), plus `httpMock.verify()`.
- `pending.component.spec.ts`: stubbed `IncidentAiService` (real one imports `firebase/app+auth`).
- `image-upload.service.spec.ts`: added `deleteDatabase` to the IndexedDB mock; `Sentry.captureException` assertion now polls via `vi.waitFor`.
- Verified: `npx prettier --check .`, `npm test -- --no-watch` (418 passed), `npm run build`.

### Deploy Functions: install, runtime, and target config

- Regenerated `functions/package-lock.json` (`npm ci` was failing on `@types/express` drift).
- `functions/package.json` engines + workflow `node-version`: 18 → 20 (deps require ≥20; 18 is EOL).
- `firebase.json`: added `functions` codebase (`source: functions`) so `--only functions` resolves.
- Verified: `npm ci`, `npm run build`, `npm test` (24 passed) in `functions/`.
