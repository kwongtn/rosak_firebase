# 2026-09-24

## Methodology: `/methodology` page, shared `app-info-popover`, disclaimer, code-first registry (6355d6b, 1bb21a0, 4af4550, 1dbb800, 22c0d66, 977e10f, 5c53c4c, 548581d, 56ac071)

- **What**: the "How this is counted" feature ships as **one registry with two readers**.
  - **Registry** (`src/app/core/methodology/`, 6355d6b): `methodology.constants.ts` holds only
    `STALE_REVIEW_MONTHS: 6` (v1 has no landed transit constants — every metric number is
    backend-owned); `methodology.content.ts` holds the 8 `METHODOLOGY_SECTIONS` and 13
    `METRIC_DOCS` (6 `line-status.*` + 7 `passenger.*`, the existing home copy folded in verbatim);
    `methodology-render.util.ts` exports `renderMethodologyCopy()` (unknown `{{TOKEN}}` **throws**),
    `methodologyTokenValues()`, and `metricDoc(id)` / `section(id)` (throw on unknown ids).
  - **Page** (`src/app/features/methodology/`, 56ac071): `/methodology` registered after `gdpr` in
    `app.routes.ts` and `RenderMode.Server` in `app.routes.server.ts`; `MethodologyPage` renders the
    `<h1>How this is counted</h1>`, a disclaimer under it and again at the end, and one
    `app-methodology-section` per registry section (anchor id, "Last reviewed", owning-route link,
    in-progress note, one popover per metric). The footer links it (1dbb800).
  - **Popover** (`src/app/ui/info-popover/`, 1bb21a0): shared `app-info-popover` extracted from the
    home status chip, fixing all five a11y defects — a real `<button>`, `role="dialog"` when the
    panel carries a link (else `role="tooltip"`), Escape-closes-and-returns-focus, outside-click
    close, `aria-controls`, and a viewport-clamped width with `align` edge flip.
  - **Disclaimer** (`src/app/ui/disclaimer-note/`, 4af4550): `app-disclaimer-note`,
    `variant: "inline" | "footer"`, no data dependencies.
  - **Home folded in** (977e10f, 5c53c4c): `PASSENGER_INFO` / `LINE_STATUS_INFO` build their
    `StatusInfo` from `metricDoc(id)`, `PASSENGER_METRIC` reads the same definitions, and
    `status-info-chip.component.ts` is now a thin wrapper over `app-info-popover`; test ids and
    visible text are unchanged. Tracker's info panel gained the rail-schedule sentence (548581d).
  - **Anti-drift** (22c0d66): new `.github/pull_request_template.md` carries the checklist line
    requiring a registry update + `lastReviewed` bump + `npm test -- --no-watch`.
- **Decisions documented**: all 8 sections render the "in-progress" state naming their owning spec,
  because DATA_PROVENANCE.md / MONTHLY_REVIEW.md / LINE_STATUS_DERIVE.md / NEXT_TRAIN_ARRIVALS.md
  declare they block this spec and their **code** has not landed (spec lines 31-32). `reliability`
  and `limitations` carry `ownerRoute: null` (their `/review` route and no route do not exist) — the
  one documented exception to "every section links to a feature route". v1 wires **no**
  `dataSources` fetch (the backend field is absent), so `sources` renders in-progress and never a
  license literal.
- **Tests**: full suite **741 tests** green; `npx prettier --check .` clean; `npm run build`
  succeeds. Drift specs cover token mutation/unknown-token throw, registry completeness, popover
  a11y, and the disclaimer copy.
- **Docs**: new `docs/components/methodology.md` (registry contract, token rule, popover guidance,
  CMS overlay escape hatch, in-progress rule, `ownerRoute: null` exception, anti-drift layers);
  `docs/COMPONENTS.md` (catalog row, topology feature list, content-model note, stale about/opportunity
  rows); `rosak_firebase/AGENTS.md` new Methodology & Metric Constants subsection + backend path fix;
  root `AGENTS.md` stale-doc fixes; this entry.

## Docs: stale-doc sweep + the `vitest --filter` case-sensitivity trap (this change)

- **What**: fixed every stale claim in METHODOLOGY_DOCS.md §Current state — root `AGENTS.md`
  (`Django 4.2` → 5.2, the two `--parallel --keepdb` test claims → the serial `--keepdb` gate,
  `9 features` → 11, trap #1 `adminOnlyGuard` → the fixed note pointing at
  `rosak_firebase/AGENTS.md:138-141`); `rosak_firebase/AGENTS.md` backend sibling path
  `/home/kwongtn/rosak_backend` → `/home/kwongtn/rosak/rosak_backend`; `docs/COMPONENTS.md` about
  row (now an in-page admin editor, not read-only) and the gdpr/about opportunity row (both pages
  now have `isError` + retry, so non-AI opportunity #4 is done and removed). Added the
  methodology-registry line to the monorepo workflow step 4.
- **Tests**: no code changed by this sweep; the same full suite (741 tests) is the gate.
- **Docs**: this entry; new `MISTAKES.md` entry for the `vitest --filter` trap that bit two agents.

## Methodology reviewer follow-ups: passenger deep-link, vehicle-count registry entry, about link (15f7bd0, this change)

- **What**: four reviewer findings on the shipped methodology feature.
  - **B1** (15f7bd0): `StatusInfoChipComponent` gained a `linkFragment` input (default
    `line-status`) and `_methodologyLink` became a computed over it; the home passenger chip passes
    `linkFragment="sightings"`, so its popover deep-links to `/methodology#sightings` (the section
    owning `passenger.*`) instead of `#line-status`.
  - **N5**: the hover-capable click test's title now states what it asserts (click-from-closed
    opens; hover+click never closes).
  - **N3**: new `MetricDoc` `line-pulse.vehicle-count` (section `line-status`, title/definition from
    the card's former literal) and the vehicle chip reads it via `metricDoc()`.
  - **N6**: `/about` gained the "How this is counted" `routerLink` to `/methodology`.
- **Tests**: `status-info-chip` + `line-pulse-card` specs gained the deep-link and registry-body
  assertions (RED → GREEN); full suite **743 tests** green.
- **Docs**: `docs/components/methodology.md` metric count + chip `linkFragment` note; this entry.

## SSR: home status chips restored after the NG0502 projection trap (f0954ac)

- **What**: `GET /` had been serving HTTP 200 with a 22-byte `Internal server error.` body (dev
  `:4200` and the prod SSR build) since `5c53c4c`; `/methodology` was unaffected. The error was
  swallowed by `@angular/ssr`'s `writeResponseToNodeResponse` bare stream catch
  (`node_modules/@angular/ssr/fesm2022/node.mjs:401-404`), so nothing was logged. Instrumenting the
  response stream exposed `NG0502: During serialization, Angular was unable to find an element in
the DOM: … #node(projection) <div popoverExtra=""> …` at
  `calcPathForNode`/`appendSerializedNodePath`/`annotateForHydration`. Root cause:
  `StatusInfoChipComponent` projects `<div popoverExtra>` into the shared `InfoPopover`'s
  `<ng-content select="[popoverExtra]" />`, which sits inside `@if (_open())` — false on the server,
  so the projected node is orphaned with no DOM counterpart and the hydration serializer throws
  mid-stream. `ngSkipHydration: ""` on the `InfoPopover` host
  (`src/app/ui/info-popover/info-popover.ts:45`) is Angular's documented remedy for
  hydration-incompatible content, and every future `[popoverExtra]` consumer (e.g. the planned
  provenance chip) is covered. Consequence: Angular's `clearElementContents` discards the host's
  children and re-renders the component, so `app-info-popover` instances re-render on hydration
  instead of hydrating — the server markup inside `app-info-popover` is newly created client-side
  (functionally identical; a11y and the public API are unchanged).
- **Tests**: new `src/app/features/home/line-pulse/status-info-chip.server.spec.ts` renders the real
  chip through the actual server path (`renderApplication` + `provideClientHydration`) and fails with
  the same `NG0502` without the fix. Full suite **86 files / 744 tests** green; `npx prettier --check
.` clean; `npm run build` exit 0; dev `/` 243,501 bytes (markers `passenger-status`,
  `line-vehicle-count`, `line-status-badge` and info triggers present) and `/methodology` 44,346;
  prod SSR `/` 264,232 and `/methodology` 66,226.
- **Docs**: `MISTAKES.md` entry for the conditional-slot projection trap;
  `docs/components/methodology.md` popover SSR note; `docs/components/home.md` chip + server-spec
  line; this entry.

## Home chips: text-pill triggers and a delayed popover close (this change)

- **What**: the home line accordion's info chips lose the "(i)" pill — `StatusInfoChipComponent`
  passes `showIcon=false`, so the projected badge is the trigger. The shared `InfoPopover` gained
  `showIcon` (default `true`, so `/methodology`'s icon-only popovers are unchanged). Hover tracking
  moved from the trigger button to the `app-info-popover` host, and leaving the host now schedules
  the close after `HOVER_CLOSE_DELAY_MS` (1000 ms) instead of closing immediately — the cursor can
  cross the few pixels to the panel and click "How this is counted". Re-entering the host cancels
  the pending close; Escape/outside-click still close (and cancel) immediately; the timer is
  cleared via `DestroyRef`. A second input, `showMethodologyLink` (default `true`), drops the link
  and demotes the panel from `role="dialog"` to `role="tooltip"`; the line-status chip passes
  `[showMethodologyLink]="false"` (line status has no method section) while the passenger and
  vehicle chips keep the link.
- **Tests**: `info-popover.spec.ts` pins the delayed-close contract with fake timers (not
  immediate; closes at exactly 1000 ms; re-enter cancels; a trigger-only leave never schedules),
  plus `showIcon=false` (no glyph, projected content still opens) and `showMethodologyLink=false`
  (no anchor, tooltip role, no tabindex). The chip/card specs assert no glyph on any home chip, the
  line-status panel has no link, and the passenger panel keeps it. Full suite **86 files / 753
  tests** green; `npx prettier --check .` clean.
- **Test-isolation fixes** (pre-existing, surfaced by the new specs' size-ordering shift): the two
  registry-mutation specs now restore `METRIC_DOCS` and `vi.resetModules()` in a `finally`;
  `line-status-sheet.component.spec.ts` stubs `matchMedia` via `vi.stubGlobal` +
  `vi.unstubAllGlobals()` so its `matches: true` no longer leaks to later files. `MISTAKES.md`
  records the class.
- **Docs**: `docs/components/methodology.md` (InfoPopover inputs + host-level hover/delay),
  `docs/components/home.md` (chip triggers + line-status no-link), `MISTAKES.md`, this entry.

## Copy: drop the schedule-derived rail claim from the disclaimer and tracker (6727641)

- **What**: removed the sentence "Rail positions and arrival times are schedule-derived, not live GPS
  or GTFS-Realtime." from the canonical `app-disclaimer-note` copy
  (`src/app/ui/disclaimer-note/disclaimer-note.ts`) and dropped the `/tracker` info panel's
  `realtime` rail paragraph (`tracker-info-panel.component.ts`): the app shows live GTFS data, not
  schedule estimates. The tracker spec's realtime-overview test and the disclaimer spec's canonical
  sentence list were updated to match.
- **Tests**: full suite **86 files / 743 tests** green at the commit; `npx prettier --check .` clean.
- **Docs**: this entry; `docs/components/methodology.md` DisclaimerNote paragraph now records the
  deliberate removal so it isn't re-added. `docs/components/tracker.md` needed no change (it never
  described the removed sentence as current behaviour).

## Popover: one panel at a time + a 300 ms hover grace (53255b4, this change)

- **What**: two behaviour changes to the shared `app-info-popover`.
  - **Exclusivity** (`InfoPopoverRegistry`, `providedIn: "root"`, same file): the registry holds the
    currently-open popover. Every open path (hover, focus, click, tap) claims the slot — closing
    whichever instance held it and cancelling that instance's pending close timer — and every close
    path (delay timer, Escape, outside click, blur-leaves-host, tap-toggle-close) releases it;
    `DestroyRef.onDestroy` releases too, so a destroyed component never lingers as the active one.
    Fixes the overlap: moving the cursor from pill A to pill B used to leave A's panel open for the
    whole grace window while B's panel opened. The registry is injector-scoped rather than
    module-level because the non-isolated test runner and SSR both share module state.
  - **Grace window**: `HOVER_CLOSE_DELAY_MS` 1000 → **300 ms** — still long enough for the pointer to
    cross the few pixels between a pill and its own panel (the reason the delay is not 0), short
    enough that pill-to-pill movement feels immediate.
  - The panel's `absolute … z-20` stacking is untouched: while the cursor is over A's open panel,
    hit-testing lands on the panel, so the pills it overlays never fire `mouseenter` and stay closed.
  - Every other semantic is unchanged: the hover-capable probe, focus-opens/blur-closes only when
    focus leaves the host, click never closes while hovering, tap toggles on touch, Escape closes and
    returns focus to the trigger, SSR renders closed and inert, `showIcon`/`showMethodologyLink`.
- **Tests**: `info-popover.spec.ts` re-pins the delay at 300 ms (fake-timer boundaries) and adds the
  two-popover exclusivity test — A opens on hover, hovering B closes A (`aria-expanded="false"`,
  panel gone) while B opens — plus a stacking pin (`absolute` + `z-20`) for the hit-testing
  mechanism. RED first (2 failing) → GREEN; full suite **86 files / 755 tests** green;
  `npx prettier --check .` clean; `npm run build` exit 0.
- **Docs**: `docs/components/methodology.md` hover bullet now states the 300 ms grace and the
  one-panel-at-a-time rule; this entry and the monthly row.
