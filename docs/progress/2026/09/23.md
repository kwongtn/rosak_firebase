# 2026-09-23

## Home: two-panel front page + 30s line-refresh countdown (commit `feat(home): split the front page into two panels with a line-refresh countdown`)

- **What**: the community front page is now split for desktop and the line data refreshes on a
  visible 30s beat.
  - **Two panels** (`home.page.ts`): the submit box, retry banner and `<app-footer />` stay full
    width; only the feed and the line-status sections moved into a responsive wrapper
    (`data-testid="home-panels"`, `flex flex-col gap-6 lg:grid lg:grid-cols-2 lg:items-start`) —
    stacked on mobile, two columns from `lg` up, URL feed LEFT and line statuses RIGHT (DOM order
    unchanged, so the submit → feed → lines ordering assertions still hold).
  - **Feed full height** (`home.page.ts`): the `feed-scroll` container lost both
    `max-h-[60vh]` and `overflow-y-auto` and renders every loaded link — the client-side reveal
    cap (`FEED_INITIAL_VISIBLE`, `visibleCount`, `visibleFeedLinks()`) was deleted. There is no
    inner scroll any more: the page itself scrolls, and `feed-count` now reports
    `store.feedLinks().length` against `store.feedTotalCount()`. Load More still pulls the next
    page (`FEED_PAGE_SIZE = 8` unchanged), and hides while a page is in flight.
  - **Lines-only polling** (`home.store.ts`): `polling` is public now (the page renders its
    countdown) and its callback is the new `reloadLines()` — `linesResource.reload()` plus a
    `linesRefreshTick` bump — instead of `reloadAll()`. The beat therefore never touches the
    feed, so a poll can't wipe the user's appended Load More pages; `reloadAll()` is unchanged
    for the submit/edit flows. `start()`/`stop()` keep the existing 30s lifecycle.
  - **Countdown** (`home.page.ts`): a row at the top of the line panel
    (`data-testid="line-refresh-countdown"`) with the spinner SVG, `Refreshing in {{ … }}s`
    (`HomeStore.polling.secondsRemaining()`) and a `Refresh now` button
    (`data-testid="line-refresh-now"`) wired to `polling.refreshNow()`. No interval select —
    the cadence is deliberately fixed at 30s (mirrors situasi-section minus its picker).
  - **Accordion refresh** (`line-pulse-list.component.ts` / `line-pulse-card.component.ts`):
    both gained `refreshTick = input(0)`; the page feeds the store's tick into the list, the list
    into every card (active and "Other lines"), and the card into the expanded panel's
    `app-line-status-chart` / `app-line-status-reports` — whose existing applied-tick effect
    re-reads their own resource while open. Feeding stops there: the two leaf components were
    not modified.
- **Tests**: `home.store.spec.ts` pins the poll callback as lines-only (tick bumps, lines
  re-requested, `httpMock.expectNone`ed feed, appended pages + cursor survive) and that
  `reloadAll()` still resets the appended pages without bumping the tick. `home.page.spec.ts`
  asserts all loaded links render, the `lg` two-column wrapper with feed-first child order, the
  uncapped feed container, the countdown text/button, and the tick handed to the list; its
  `StoreMock` gained `polling` + `linesRefreshTick`. List/card specs pin tick forwarding.
  Scoped vitest: 4 files / 51 tests green; `npx prettier --check .` clean.
- **e2e**: `e2e/home.spec.ts`'s stale "under the scroller" comment corrected; its
  `feed-count` (`Showing 1 of 9`), `feed-load-more` and `other-lines` selectors are unaffected by
  the restructure (same rendered content, new container).
- **Docs**: `home.md` updated (layout, polling bullet, `refreshTick` inputs, extension points).

## Home: recent-reports list capped in its own scroll container (commit `4c92101`)

- **What**: `LineStatusReportsComponent` now wraps its loaded rows in a capped scroll container
  (`data-testid="line-status-reports-scroll"`, `max-h-56 overflow-y-auto pr-1`) so the expanded
  line card's report list shows roughly five one-line rows and scrolls internally instead of
  stretching the card. Rows carrying `notes` run taller, so the cap is approximate. Only the loaded
  branch is wrapped — the skeleton, empty and error branches render outside the scroller.
- **Also in this commit**: the component gained the `refreshTick = input(0)` + applied-tick effect
  that lets the parent's poll beat re-read the list while the accordion is open (the same guard as
  `LineStatusChartComponent`, added one commit earlier). Not a model/query change.
- **Tests**: `line-status-reports.component.spec.ts` pins the scroll container, its cap classes and
  the skeleton branch outside it.
- **Docs**: `home.md` `LineStatusReportsComponent` bullet updated; this entry.

## Spotting: a cleared combobox / station select really deselects (commit `1f37d7d`)

- **What**: emptying a shared `HlmCombobox` now clears the selected value instead of snapping the
  old label back on blur. The control gained an `emptyValue` input (default `undefined`);
  `_onInput` writes it when the text is emptied, and `_syncSearchToValue` short-circuits on
  `undefined` so it can no longer resurrect a cleared field. The report form's line and vehicle
  pickers bind `emptyValue=""` (string-typed Signal Forms fields must, because Signal Forms drops a
  node whose model becomes `undefined` while its control is rendered), and the three station
  `<select>` placeholder options lost `disabled`, so picking `Select a station` clears the station.
- **Why**: the typed text and the committed value are separate states in a combobox; clearing the
  text never cleared the `[formField]`-bound `value`, so the field looked cleared but kept
  submitting the old id.
- **Tests**: `ui/combobox/combobox.spec.ts` covers clear-on-empty (and non-empty mid-search) and
  `report-form.component.spec.ts` the placeholder-clear path; the three consumers (report-form,
  vehicle-detail, calendar) already guard falsy values.
- **Docs**: `spotting.md` form-state bullet + `MISTAKES.md` updated; this entry.

## Home: the URL form heads the desktop left column; the feed gains empty and skeleton states (dbacb1c)

- **What**: `home.page.ts` renders `app-link-submit-box` as the first child of the feed column,
  inside `home-panels` and ahead of the list in DOM order — previously it sat full width below the
  nav. On desktop the URL form now heads the column it feeds; while the panels are stacked on
  mobile it stays full width. The feed settles into one of three states when it has no rows: the
  `feed-skeleton` (`data-testid="feed-skeleton"`, `hlmSkeleton h-24 w-full`) while the first page
  loads, the muted `feed-empty` (`data-testid="feed-empty"`, "No links yet.") once the read is
  settled and error-free, and neither (the retry banner instead) when the read failed. `feed-empty`
  reuses the line list's empty-state shell (`text-muted-foreground` + `border-dashed`) so the two
  columns read alike; `feed-footer` stays hidden until the feed has at least one row.
- **Tests**: `home.page.spec.ts` pins the submit box ahead of `app-link-card` inside the feed
  column, the empty state's exact copy and dashed/muted shell, the skeleton-not-empty case while
  loading, and that the empty state stays off a feed that has links or errored.
- **Docs**: `home.md` (feed states + `feed-skeleton`); this entry.

## Home: vehicle-count badge, non-ACTIVE-only status pill, consolidated message removed (b1bde7c, b5cee5a)

- **What**: the line pulse card's status row (`line-pulse-card.component.ts`) replaced the inline
  "N of M vehicles in service" text with a `line-vehicle-count` `hlmBadge` pill reading
  `{{inService}}/{{total}} in service` (e.g. "12/20 in service") carrying
  `aria-label="N of M vehicles in service"`; its `app-status-info-chip` popover keeps the per-status
  breakdown and adds a derived `Total` row. The line-status pill now renders only when
  `line().status !== "ACTIVE"` — "Active" is the unremarkable default and no longer takes a chip.
  The passenger chip no longer renders the backend's consolidated `passengerStatusMessage`
  ("According to N social media entries…"); the query still selects it and
  `StatusInfoChipComponent.message` still supports it, but no card passes a message.
  `status-info-chip.component.ts`'s "Last N minutes" window line gained `mt-3` so a blank line sits
  above it.
- **Tests**: `line-pulse-card.component.spec.ts` pins the badge text, its `aria-label`, and its
  position as the last chip in the row, the breakdown rows + `Total`, the hidden Active pill (and
  the pill still shown for `PARTIAL_DISRUPTION`), and that no `status-info-message` renders on the
  card or in the popover. `e2e/home.spec.ts` asserts `12/20 in service` / `4/12 in service` and no
  "Active" text.
- **Docs**: `home.md` card bullet; this entry.

## UI: a cleared combobox no longer re-commits on Enter (22add7c)

- **What**: `ui/combobox/combobox.ts`'s `_selectHighlighted()` now commits only what the user
  actually chose — a row reached by deliberate ArrowUp/ArrowDown navigation (`_hasMovedHighlight`,
  set by `_moveHighlight`, reset on open/input) or, failing that, a non-empty query's highlighted
  match. After the field is cleared the query is empty and no row was navigated to, so Enter is a
  no-op and the panel stays open instead of committing `_filtered()[_highlightIndex()]`.
- **Why**: the earlier clear fix (`1f37d7d`) cleared `value` on empty text, but left the panel open
  over the now-UNFILTERED list (`_filtered()` returns every item for an empty query) with the old
  highlight index intact — so pressing Enter after clearing re-selected the old item, or a
  different one. The first fix's spec cleared the field and then BLURRED, the one gesture that
  closes the panel, so it never exercised the Enter that follows the clear. That is why the bug
  survived the first fix and was reported twice. See `MISTAKES.md`.
- **Tests**: `combobox.spec.ts` gains three cases — Enter after clear is a no-op, Enter on a
  click-opened panel with no query or navigation is a no-op, and Enter after an ArrowDown still
  commits.
- **Docs**: `spotting.md` form-state bullet; `MISTAKES.md`; this entry.

## Spotting: the station resource projects its line/type dependencies (6bbb2a0)

- **What**: `report-form.component.ts`'s `stationLinesResource` requestFn no longer reads the whole
  `model()` signal. It reads two `computed`s of primitive values — `_stationLineId`
  (`model().lineId`) and `_stationType` (`model().type`) — and keeps `variables: { lineId }` as
  before, so the resource re-issues `StationLinesByLine` on a line or spotting-type change but on no
  other model write.
- **Why**: a `graphqlResource` tracks every signal read inside its requestFn, so reading the whole
  model made every write a dependency. Typing in Notes re-fired the station query on each
  keystroke — measured at 10 keystrokes = 10 `StationLinesByLine` POSTs — even though only
  `lineId`/`type` participate in the request.
- **Tests**: `report-form.component.spec.ts` pins it — an unrelated `notes` write triggers no
  station POST, a `type` change still refetches.
- **Note for spec authors**: jsdom/vitest flush the resource synchronously, so a unit test cannot
  observe the refetch-storm timing window a real browser with real latency exposes. The spec asserts
  the dependency projection (no request on an unrelated write), not an observed call count over
  time; reproduce the storm in a browser. See `MISTAKES.md`.
- **Docs**: `spotting.md` form-state bullet; `MISTAKES.md`; this entry.
